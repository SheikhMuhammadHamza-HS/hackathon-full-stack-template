[project]
name = "todo-console-app"
version = "0.1.0"
description = "Console-based todo application for Phase I"
readme = "README.md"
requires-python = ">=3.13"
license = {text = "MIT"}
authors = [
    {name = "Todo App Developer", email = "dev@example.com"}
]
dependencies = [
    "questionary>=2.1.1",
    "rich>=14.2.0",
]
